# Current State

Updated: 2026-09-20

Published seven evidence-backed travel articles based on Jack's May 2026 Europe trip across Geneva, Venice, Slovenia, Austria, Slovakia, Czechia, Poland, and Ireland. Every route, ticket, and timeline stop was audited against Jack's actual downloaded transit tickets (FlixBus, ÖBB Sparschiene, KLM Cityhopper, easyJet, ATVO) and Google Maps breadcrumbs, correcting an earlier assumption about transit from Venice to Slovenia (confirmed as direct FlixBus Route 403, Seat 1A from Venice Mestre). Each article addresses a specific search query with responsive comparison tables, route timings, and authentic photos pulled from Jack's camera roll:
- `/blog/venice-mestre-to-ljubljana-bus/` (FlixBus Route 403 booking, Viale Stazione 8/8c stop, seat 1A, border checks)
- `/blog/ljubljana-to-graz-train/` (ÖBB IR 576 Sparschiene 29.90 EUR booking, station platforms, 46.6 kg CO2 savings)
- `/blog/graz-to-bratislava-bus/` (FlixBus Route 885 departure from P&R Webling vs Graz Hbf confusion, transit to Erna-Diez-Straße)
- `/blog/bratislava-to-brno-bus/` (FlixBus Route 260 to Brno Hotel Grand vs train, Brno 20/25/27/100 CZK transit fares)
- `/blog/geneva-airport-layover-walk/` (3-hour layover walk from Gare Cornavin to Bains des Pâquis and Jet d'Eau, Swiss SIM card)
- `/blog/wroclaw-to-dublin-flight-schiphol-layover/` (KLM Cityhopper KL1336/KL1137 with 2-hour Amsterdam transfer, Schengen exit border control)
- `/blog/dublin-authentic-pubs-mulligans-gravediggers/` (Mulligans on Poolbeg Street and John Kavanagh The Gravediggers in Glasnevin, bus 83, etiquette)

All seven articles strictly adhere to the plain article layout and domain ban on em dashes and en dashes. Commit `05ed30f` is pushed to `main`.

Removed the dating quiz link from the global header navigation in `src/components/Header.astro`, so it no longer appears on the homepage or across the site. The route `/compatibility-quiz/` remains active as a standalone page.

All 26 AI Audit ad assets were removed from `public/meta-ads/ai-audit/` on 2026-09-06. The deletion is committed as `94c60fb` and pushed to `main`, removing approximately 59.72 MB from future static builds. The local Astro build still completes route generation and fails only at the known Windows `@astrojs/vercel` Sharp symlink packaging step.

The Philadelphia happy-hour page now switches its wide tables to stacked, labeled rows at mobile widths. At 390px the live page has no horizontal overflow, preserves all 12 tables and 34 deal rows, and keeps the original table layout on desktop. Commit `85db404` is pushed to `main` and live.

The Appalachian Trail source archive is now published in Notion under the existing Master Wiki at the Appalachian Trail Thru-Hike Wiki page. It includes 77 dated AT Blog journal pages, 98 searchable daily distance pages, overview and stats, stats and graphs, the complete gear table, the original ATDistanceChart.xlsx attachment, the slide deck links, and a source index. The source material came from Google Drive because no local AT journal file was found in the searched user folders. The workbook reports 2,178.3 cumulative miles, while its displayed daily rows sum to 2,178.2 because the row values are rounded.

The compatibility quiz now has 16 ranked-choice relationship-style questions plus two separate practical fit checks. The preference questions cover warm banter, direct conflict, ordinary-life comfort, warmth, specific attention, intimacy, pacing, independence, intellectual range, family integration, reciprocal effort, accountability, rooted novelty, unimpressibility, romantic pull, and wanting to repeat ordinary life. The practical checks cover long-term intent and openness to dating a man who is 5'3".

The compatibility quiz v5 revision is implemented locally but not deployed. The questions render as ranked-choice controls with shuffled display order, rank-distance scoring, weighted compatibility categories, core flags, and duplicate-rank validation. Practical mismatches are reported separately without changing the relationship-style score, and one weak core answer prompts investigation rather than an automatic rejection. The matchmaker wiki is hidden until quiz completion. The single visible contact form is included in completed Web3Forms submissions with raw ranks, computed scores, practical-fit fields, quiz version, timestamp, referrer, and UTM fields. Direct Astro compilation and prerendering succeed; final local Vercel packaging remains blocked by the known Windows Sharp symlink permission error. The pnpm wrapper also attempted a dependency reinstall and reported ignored native build scripts.

The live compatibility quiz now uses 16 forced-choice preference tradeoffs. Each non-height question offers four reasonable relationship styles, one of which matches Jack's specific preference; nonpreferred answers receive a neutral baseline rather than being treated as unhealthy. The preferred position is balanced evenly across A, B, C, and D. The height question keeps an honest dealbreaker at zero because it is practical incompatibility. The copy passed the four Notion AI Slop failure-mode gates and the live page and quiz bundle were verified after deployment.

The compatibility quiz mobile rendering was fixed on 2026-08-20. Its dynamically inserted question and report markup now receives global styling, so answer choices render as separate bordered cards with usable tap areas instead of collapsing into inline text. The live route was retested after deployment, including selecting all 16 answers and generating the report.

The public `/compatibility-quiz/` route is implemented and linked from the global navigation. It uses 16 behavior-based questions, including a neutral question about dating a guy under 5'5", then produces a local browser-only compatibility report across banter, clarity, pacing, curiosity, ordinary-life fit, attraction, and other categories. The page includes the full matchmaker wiki supplied by Jack. The route prerendered successfully; final local Vercel packaging still hits the known Windows Sharp symlink limitation.

The interactive article `/blog/how-chatgpt-sees-new-york-city/` is ready for publication. It uses all 262 NYC Planning 2020 Neighborhood Tabulation Area polygons, grouped into 25 mutually exclusive visual clusters covering all five boroughs. Hover, tap, select, and keyboard controls swap among 25 locally stored ChatGPT-generated neighborhood portraits. The page explains the prompt, synthetic subjects, model stereotype risk, editorial clustering, and official geometry source. It is registered automatically in the homepage, Writing archive, RSS feed, sitemap, canonical metadata, Open Graph metadata, and Article schema. Local Astro prerendering succeeded, while final Vercel function packaging hit the documented Windows Sharp symlink limitation.

The `/Your-AI-Audit/` hero now leads with the buyer's actual pain and unresolved intent: `Still losing time to a repetitive task you know AI could make faster?` The supporting copy distinguishes the steps AI can help with from those that should remain manual, and the closing CTA now reinforces finding the first workflow to test. Commit `07761c3` is live. The complete checkout measurement block remained identical.

The `/Your-AI-Audit/` DIY objection section now explains the practical basis for Jack's fast recommendation: more than 300 million agentic AI tokens used across job application workflows, hundreds of customized emails, websites, event plans, business plans, and custom skills and agents for business automation. The proof is tied to reducing trial and error rather than presented as a standalone metric. A quiet `Jack on LinkedIn` identity link now appears in the footer. Commit `1d1c3f1` is live, and the checkout measurement block remains unchanged.

The `/Your-AI-Audit/` landing page now opens with the same problem, 45-minute qualifier, deliverable, price, delivery clock, and refund condition as the active repetitive-task ad. A visually secondary Web3Forms question form sits after the final checkout block for visitors who are unsure whether their task fits. It collects only name, email, and a question, warns against private data, and does not subscribe visitors to a list.

On 2026-08-11, the live AI Audit attribution path was corrected after same-day GA4 reporting showed paid-social visits duplicated as Unassigned sessions. The browser bootstrap now resolves GA4's real client ID and session ID from current GS2 or legacy GS1 cookies, with `gtag('get')` as a fallback, and server-side audit events carry standard campaign, page-location, and referrer fields. CTA and checkout-start delivery is awaited before the Stripe redirect so the pair cannot drift because of navigation timing. Commit `070cf43` is deployed. The focused GS1 and GS2 identity test passes, live production assets contain the new helper, and an internal QA checkout preserved the full UTM set plus compact Meta and GA client reference. GA4 DebugView remains empty for labeled QA because the active Internal Traffic filter excludes those events. Confirm the absence of new Unassigned duplicates against fresh real paid traffic rather than disabling that filter.

The `/Your-AI-Audit/` conversion page now sells one plain product: the $59 One-Task AI Plan. A buyer submits one recurring computer task through an eight-question intake and receives one personally reviewed, one-page PDF with the current workflow, one selected recommendation, projected savings and software cost, three setup steps, and the first test. The PDF is due by the end of the next business day after the completed intake. Installation, account access, custom software, and achieved results are outside the offer. If the completed intake cannot support a practical change projected to save at least 30 minutes per week, Jack initiates a full refund without the buyer asking. The audience includes work and personal admin, while physical chores, one-off work, broad audits, sensitive records, and custom builds are excluded. The $59 amount is a launch-price prior chosen to favor early paid Purchase volume while retaining most modeled front-end revenue per $10,000 of ad spend. It is not an empirically optimized price because there were zero live purchases when it was selected.

Three text-led Meta ad concepts now match this contract, each with exact 1080 by 1080 and 1080 by 1920 assets. The vertical files reserve the bottom 20 percent and 10 percent side zones from readable text. Stable concept keys are `aa59_c01_task_ledger`, `aa59_c02_weekly_question`, and `aa59_c03_one_page_plan`. The first paid test should use a hard loss limit of no more than $590, and likely less while Jack is unemployed. Pause at 100 qualified landing visits with no purchase instead of treating $10,000 as a starting budget.

The audit funnel sends browser view, CTA, checkout, verified return, and intake events through the allowlisted Vercel `/api/ga4-audit-event/` endpoint with a stable GA4 client and session identity plus campaign, ad, placement, and landing-session identifiers. The public route cannot create `purchase`, `refund`, or delivery events. Signed Stripe webhooks remain the only source of paid Purchase and now also handle the Stripe refund lifecycle for GA4 net-revenue reporting when the original transaction can be mapped. The paid intake verifies the Checkout Session server-side before it enters the verified denominator. If the restricted Stripe key is unavailable, the customer sees a clearly labeled pending-manual state and the form does not emit a verified completion. GA4 Admin keeps only `purchase` as a key event; generic `click` and `scroll` are not key events. The existing Internal Traffic data filter is active in Exclude mode.

The One-Task AI Plan has a live Stripe one-time $59 Price and hosted Payment Link. The former $79 Payment Link is inactive. The `/api/stripe-ai-audit-checkout/` route creates a hosted Checkout Session from that Price and writes campaign, ad, placement, landing, Meta, and GA identifiers into Checkout Session and PaymentIntent metadata. The signed purchase webhook reads those values back into GA4 and Meta and uses the paid Session's actual amount. During the launch cutover it accepts both 5900 and 7900 for matching Snapshot metadata so cached $79 checkouts cannot lose attribution. Dynamic Session creation and automatic paid-intake verification require the missing Vercel secret `STRIPE_AI_AUDIT_RESTRICTED_KEY`. Until a least-privilege key is explicitly authorized and configured, the CTA safely falls back to the current $59 Payment Link, preserves the compact Meta and GA identifier reference, and treats the returned intake as pending manual payment verification. The live Stripe Product display name is `One-Task AI Plan`, its existing Product, Price, and Payment Link IDs are unchanged, and the active signed webhook now subscribes to Checkout completion plus `refund.created`, `refund.updated`, and `refund.failed`.

Live funnel QA on 2026-08-10 confirmed that Ads Manager contains exactly three AI Audit ads and that each ad has three selected media variants for square, horizontal, and vertical placements. The live page, $59 Stripe Checkout, Meta PageView and InitiateCheckout, and GA4 view-item and checkout events worked. Keep the campaign paused until the remaining launch gates pass: ads 1 and 3 should use Get Offer instead of Learn More, the restricted Stripe key should activate dynamic Checkout and full attribution metadata, Meta ViewContent should appear in Test Events, and one current $59 test payment should produce exactly one signed Meta CAPI Purchase and one GA4 purchase with the expected value, currency, transaction ID, and item. The detailed report is `.gstack/qa-reports/qa-report-ai-audit-measurement-2026-08-10.md`.

A fresh $59 Stripe sandbox payment completed on 2026-08-10 using the standard 4242 test card. Stripe event `evt_1U35JrFVkBlWBiJ6krYcXwxY` reached the signed test webhook and returned HTTP 200 with `serviceTier=snapshot`, `meta.sent=true`, and `ga4.sent=true`. After temporarily aligning the Vercel Meta test code, Events Manager visibly showed the processed server-side Purchase with value 59 USD, content ID `ai_bottleneck_snapshot`, and the stable Stripe Checkout event ID. The original Meta test code `TEST97456` was restored and redeployed. GA4's Internal Traffic filter was temporarily disabled for the resend, then restored to Active Exclude. DebugView did not show the resent historical transaction, so final GA4 interface confirmation requires a fresh sandbox Checkout Session rather than another resend with the same transaction ID.

Stripe sends paid completions for all three AI Audit tiers to `/api/stripe-ai-audit/` through a narrowly scoped live webhook. The endpoint verifies Stripe's signed payload, accepts only completed or asynchronously paid Checkout Sessions, checks live mode, paid status, the exact tier amount, USD currency, and matching offer metadata, then sends one server-side Meta `Purchase` event to dataset `1578848813945108` using the Stripe Checkout Session ID as the stable event key. Checkout links carry the first-party Meta browser and click identifiers into the paid Session so the Purchase can be matched back to the ad visit. Unpaid, mismatched, duplicated, unrelated, or unsigned events cannot be promoted into purchases. A controlled test using Meta Test Events code `TEST97456` passed on 2026-08-07, and production was returned to live event mode immediately afterward. Meta Graph calls use v25.0. The privacy policy covers payments, assessment responses, Stripe, Web3Forms, Vercel, Google Analytics, and Meta business tools.

The Snapshot guarantee covers the opportunity identified by the assessment, not results dependent on customer implementation. Checkout clicks send `InitiateCheckout`, while only Stripe-confirmed paid Sessions create `purchase` events. The route prerenders successfully. The documented Windows Vercel adapter symlink limitation still affects final local function packaging after route generation, so publication uses the normal GitHub to Vercel pipeline.

Sitewide Meta event quality was tightened at the same time. A three-page, two-minute visitor now sends the custom `EngagedVisitor` event instead of `Lead`, and an Instagram clickout sends the custom `InstagramClickout` event instead of `Lead`. Newsletter submissions remain leads, and the existing paid meet match remains a purchase.

The root-level plain article `/bottleneck/` is ready for publication. It presents a purposive, coded corpus of 100 public Substack articles about non-capability AI bottlenecks and is registered with the standalone article feed, so it appears on the homepage and `/blog/`.

The private Amex candidacy page is implemented at `/amex-nyc-hireme/`. It is excluded from sitemap generation, blocked in `robots.txt`, and protected with page-level and Vercel header-level noindex directives. It uses American Express Business-inspired blue, deep blue, neutral color, modular layout, and Benton Sans fallback styling without using official Amex logos or implying endorsement.

The Astro site deploys from GitHub to Vercel on pushes to `main`. Existing agent rules define writing, layout, build, deployment, and live-check requirements. Canonical persistent-state navigation is now present. Vercel Web Analytics is installed through `@vercel/analytics` and rendered in the shared `BaseHead` component, so routes using that head will report visitors and page views after the next production deployment.

Outstanding pages and platform identifiers remain listed in `AGENTS.md`. Update this file when deployment behavior, active migrations, or major blockers change.

The Stripe AI Audit sandbox path is now configured and verified. A test Payment Link for the $79 Snapshot redirects to the paid intake, a sandbox webhook at `/api/stripe-ai-audit/` accepts signed test events, and a Stripe Test Mode payment returned 200 OK after the endpoint URL was corrected to include the trailing slash. Meta Test Events received a processed server-side `Purchase` with the expected $79 value and `ai_bottleneck_snapshot` content ID. The test webhook and Meta test event code remain available for future QA; live webhook validation remains strict and separate.

The same sandbox Checkout Session was taken through all four intake steps with labeled dummy business data. The real Web3Forms submission completed, the page showed the Snapshot confirmation, and Meta Test Events received a processed `AuditIntakeCompleted` browser custom event with an event ID tied to the test Checkout Session. No real audit deliverable should be produced from that submission.

The standalone article `causal-inference-with-AI-for-beginners` is ready for publication at its requested root URL. It uses the normal plain article layout and has passed the site build and indexing checks.

The legacy routes `/start/` and `/projects/` return HTTP 301 redirects to `/about/` and `/blog/`, respectively, through Vercel route rules.

The canonical identity page is `/about/`. Site navigation and article bylines use that URL, and Vercel redirects legacy WordPress dated posts, category archives, and the former Appalachian Trail post into their current canonical destinations.

The professional career page is implemented at `/professional-about-me/`. It is based on a review of 47 tailored role records from the ResumeAutomation project and emphasizes the recurring hiring signals across those roles: paid social ownership, measurement, creative testing, budget accountability, profitable acquisition, cross-functional leadership, signal quality, hands-on execution, and practical AI operations. It keeps the normal site header and footer and includes a bespoke social preview.

The page is linked from the global navigation as `Professional Bio`. Its contact address is rendered only as a raster image, with no address in HTML, metadata, links, filenames, or alternative text. The page explicitly permits indexing and rich snippets, appears in the sitemap, and is allowed for major search and AI crawlers in `robots.txt`.

The ProfilePage structured data on `/about/` and `/professional-about-me/` identifies its `mainEntity` as a Person, rather than an untyped ID reference. This addresses the Search Console invalid `mainEntity` object-type report for those two URLs.

The new plain Markdown essay `kind-rejection-approach` is drafted at `src/content/blog/kind-rejection-approach.md`. It uses Notion context about dating, ambiguity, optimization, and digital contact, and links to a supporting research ledger at `research/kind-rejection-approach.md`. Local content checks passed for prohibited dash characters and whitespace. The local environment did not expose Node or npm, so Astro build and indexing checks remain pending.

The AI Audit landing page now includes a transparent process preview immediately before the Snapshot offer. A hero link jumps to browser-style previews of the real Stripe checkout, the eight-question intake, and an illustrative one-page Snapshot report. The section states what the buyer will and will not need to provide, labels sample numbers as illustrative, and makes the five-minute intake expectation explicit. All three Meta ad concepts now have square, vertical, and exact 1920 by 1080 landscape PNGs in `public/meta-ads/ai-audit/`.

The `/Travellist/` page is live as a navbar page and writing-list entry. It now follows the live NomadMania profile's 137 visited region IDs, omits Brussels and New Brunswick at Jack's request, and displays 135 normalized rows. Single-city regions remain city names, while multi-city regions use the NomadMania regional label. Flanders remains because the visited list includes Antwerp, Ghent, and Leuven. Search, country filtering, responsive rows, and Wikipedia links remain. All 141 unique rendered Wikipedia targets were checked against Wikipedia's API; generic or unrelated targets now resolve to verified containing regions, states, or countries, with unmapped labels falling back to their verified country parent. The page is organized into Europe, North America, South America, and Asia, each with a locally saved, authenticated NomadMania highlight capture and source caption. The NomadMania map embed has been removed. The Windows Vercel adapter still reports its known symlink permission error after route generation.

The compatibility quiz at `/compatibility-quiz/` now uses a mobile-first editorial form treatment curated with Impeccable. The 16 questions, answer order, preference scores, and report thresholds are unchanged. Answer labels use left-side radio affordances, full-width tap targets, visible focus states, and safe text wrapping. The matchmaker wiki is structured as a varied reference document instead of a repeated card grid. A 390px browser run selected all 16 answers, produced the expected 63 score and Mixed fit report, and found no clipped labels or horizontal overflow. The Impeccable detector returned no findings. The route prerendered successfully before the known Windows Sharp symlink packaging failure.

A current Philadelphia happy-hour research pass is saved at `research/philly-happy-hours-2026.md`. It covers official 2026 deal hours, drink and food prices, restrictions, addresses, the active Fishtown TAPS program, Google Maps closure checks, and storefront-photo leads with rights cautions. The simple site page is implemented at `/recommendations/philly-happy-hours/` with 34 venue rows, Google Maps links, and five visually inspected official exterior photos. Commit `4aec999` is pushed to `main`, and the production route is live. Local Astro route generation succeeds before the known Windows Vercel Sharp symlink packaging error.

The Philadelphia happy-hour research now includes expanded neighborhood sections for Fishtown, University City, and Northern Liberties. Added venues include Fette Sau, Frankford Hall, Dock Street Fishtown, Picnic, CO-OP, White Dog, Louie Louie, Corio, New Deck Tavern, Yanaga, Urban Village, Yards, SET NoLibs, and Jerry's Bar, with official versus secondary evidence called out where relevant.

The Philadelphia happy-hour page was rendered locally at desktop and 390px mobile widths. It uses basic Arial text, white background, black text, one-pixel table rules, horizontal table scrolling on small screens, direct venue and Google Maps links, and no generated graphics. Only Sampan, Double Knot, Harp & Crown, The International Bar, and Urban Village use images, because those five official-hosted files were visually confirmed as storefront or exterior photos. The Google Maps checks on all 34 venue rows found matching listings with no permanent or temporary closure notices on September 3, 2026.

The new plain Markdown review `inti-kitchen-bar-minneapolis-review` is ready at `/blog/inti-kitchen-bar-minneapolis-review/`. It includes five supplied photos, preserves Jack's 4 out of 5 rating and dish judgments, and fact-checks the restaurant's late-August 2026 opening, Kingfield location, menu naming, prices, ingredients, and serving options. A local build generated the route, homepage, blog archive, RSS feed, and sitemap entry successfully.

The Ox Tavern order guide is live at `/blog/the-ox-tavern-brooklyn-order-guide/` in the plain article layout. Commits `91d90b5` and `b97202f` reduced the original custom page to the three dishes, useful modifiers, one dipping note, and the estimated total for two people. The standalone Astro route and duplicate Markdown source carry matching titles, descriptions, tags, and body copy. Live checks confirmed the short order, canonical metadata, Article schema, and `More writing` footer link.
